// ✂️ Extraer una parte de una frase
// Tienes la frase "Los gatos son geniales". Extrae y muestra solo la parte "gatos" utilizando el método adecuado.

let texto = "Los gatos son geniales";
let parte = texto.slice(4, 9); // "gatos"

console.log("✂️ Extraído:", parte);
